\entete{FOD}{9 février 2013}

\section{Alg\`ebre relationnelle et SQL (10 points)}

Soit la base de données suivante (simplifiée) de gestion du 22ème Open International d'échecs de la ville de Cnamville :\\

\texttt{PARTICIPANT (idParticipant, nom, prénom, age, nationalité);\\
PARTIE (idBlanc, idNoir, ouverture, résultat);\\
classementELO (idJoueur, coeffELO);\\
}
Les attributs idBlanc et idNoir représentent les identifiants des joueurs qui jouent avec les blancs et, respectivement, les noirs. Le coefficient ELO représente la force de jeu du joueur, donnée par ses résultats dans le passé. Le champ résultat signifie le résultat d'une partie : 1 si le blanc gagne, -1 si le noir gagne et 0 en cas de nullité. A ce tournoi on ne peut avoir que des joueurs de force plus grande de 2600 ELO (niveau grand maître).

\begin{enumerate}
  \item (1 point) Pour chaque table, identifier les clés primaires et les clés étrangères. (1 point)
\begin{correction}
PARTICIPANT : idParticipant clé primaire\\
PARTIE : couple (idBlanc, idNoir) clé primaire ;  idBlanc et idNoir clés étrangères.\\
classementELO : idJoueur clé primaire et clé étrangère
\end{correction}

  \item (1 point) Donner la commande SQL permettant de créer la table PARTIE. (1 point)
\begin{correction}
\begin{lstlisting}
CREATE TABLE PARTIE (
idBlanc INTEGER NOT NULL,
idNoir INTEGER NOT NULL,
ouverture VARCHAR2(40),
résultat INTEGER,
CONSTRAINT partie\_pk PRIMARY KEY (idBlanc, idNoir),
CONSTRAINT partie\_blanc\_fk FOREIGN KEY idBlanc
REFERENCES PARTICIPANT (idParticipant),
CONSTRAINT partie\_noir\_fk FOREIGN KEY idNoir 
REFERENCES PARTICIPANT (idParticipant))
\end{lstlisting}
\end{correction}
~\\
Pour les questions suivantes, indiquer pour chaque expression donnée une requête en algèbre et/ou en SQL~: 

\item Donnez les résultats des parties dont l'ouverture est la ``Défense française''. Algèbre et SQL. (1 point)
\begin{correction}
Algèbre :  (0.5 points)\\
$\pi_{resultat}(\sigma_{ouverture='Defense Francaise'} (PARTIE))$\\
SQL  (0.5 points):
\begin{lstlisting}
SELECT résultat 
FROM PARTIE
WHERE ouverture = ``Défense française'';
\end{lstlisting}
\end{correction}
\item Donnez les noms de joueurs de force de jeu de moins de 2700 ELO qui ont gagné avec les blancs contre la ``Défense française''  (gagné avec les blancs une partie dont la l'ouverture est la ``Défense française''). Algèbre et SQL. (2 points)
\begin{correction}
Algèbre (1 point):

SQL (1 point):
\begin{lstlisting}
SELECT nom
FROM PARTICIPANT, PARTIE, classementELO
WHERE idBlanc = idJoueur AND idBlanc = idParticipant
	AND ouverture = 'Defense française'
	AND coeffELO $<$ 2700
	AND résultat = 1 ;
\end{lstlisting}
\end{correction}

\item Quelle est la force de jeu moyenne (coeffELO) des joueurs français qui ont moins de 30 ans ? SQL seulement. (1 point)
\begin{correction}
\begin{lstlisting}
SELECT AVG(coeffELO)
FROM classementELO, PARTICIPANTS
WHERE idParticipant = idJoueur AND nationalité = 'français' AND age < 30 ;
\end{lstlisting}
\end{correction}

\item Donnez les noms des participants qui n'ont jamais gagné avec les blancs contre Garry Kasparov. Ces participants n'ont pas forcément joué contre Kasparov. SQL seulement. (2 points)
\begin{correction}
\begin{lstlisting}
SELECT NOM FROM PARTICIPANT, PARTIE
WHERE idBlanc = idParticipant AND idBlanc not in 
	(SELECT idBlanc FROM PARTICIPANT, PARTIE
	 WHERE idNoir = idParticipant AND nom = 'Kasparov' AND 
	 AND prénom = 'Garry'  AND résultat = 1)
\end{lstlisting}
La partie imbriquée représente les joueurs qui ont gagné au moins une partie avec les blancs contre Garry Kasparov.
\end{correction}

\item Donner la nationalité qui a le plus de participants ayant gagné avec les noirs. SQL seulement. (2 points)
\begin{correction}
SELECT nationalité FROM PARTICIPANT, PARTIE
WHERE idParticipant = idNoir AND résultat = -1
GROUP BY nationalité 
HAVING COUNT(*) = 
	(SELECT MAX(COUNT(*)) FROM PARTICIPANT, PARTIE 
	 WHERE idParticipant = idNoir AND résultat = -1
	 GROUP BY nationalité)
\end{correction}
\end{enumerate}
 
 
\section{Index (3 points)}

Créer l'index B+Tree d'ordre 2 sur l'attribut CoeffELO avec les valeurs suivantes (s'il y a des doublons, on fera apparaître chaque valeur) :\\
2800, 2900, 3000, 2600, 2700, 2950, 2820, 2850, 2870, 2910, 2880, 3100, 3200, 2975, 2990
\begin{correction}
\begin{figure}[htbp]
\begin{center}
\epsfig{file=Fig/2013_02_btree, width=15cm}
\end{center}
\caption{Arbre B+ d'ordre 2.}
\label{fi:sol_Bp}
\end{figure}
\end{correction}

\section{Optimisation  (4 points)}

\begin{enumerate}
  \item (1 point) Pour la requête ci-dessous, dire ce quelle produit comme résultat (sous forme de phrase)
\begin{lstlisting}
  SELECT P1.NOM
  FROM PARTICIPANT P1, classementELO c1, classementELO c2, PARTIE PA
  WHERE P1.idParticipant = c1.idJoueur AND
        PA.idBlanc = P1.idParticipant AND
        PA.idNoir = c2.idJoueur AND
        (c2.coeffELO - c1.coeffELO) > 300 AND
        résultat >= 0;
\end{lstlisting}~
\begin{correction}
Cette requête donne les noms des joueurs qui ont gagné ou fait nulle en tant que blanc contre un joueur ayant 300 ELO de plus que lui.  
\end{correction}

\item (3 points) Donner un plan d'exécution (EXPLAIN \underline{ou} arborescent) pour cette requête,
sachant qu'il existe un index sur l'attribut {\tt idJoueur} de la table {\tt classementELO}, et sur l'attribut {\tt idParticipant} de la table {\tt PARTICIPANT}. Toutes les tables sont de petite taille.
\begin{correction}
3 jointures. On peut utiliser 2 jointures par Boucle imbriquée avec index.
\end{correction}
\end{enumerate}	


\section{Concurrence (3 points)}

Soit l'exécution concurrente suivante (les verrous se posent au niveau du n-uplet)~:

$$
H: r_1[x] r_2[x] r_3[y] w_1[x] w_1[z] r_3[z] C_1 w_2[z] C_2 w_3[x] C_3
$$


\begin{enumerate}
\item (1 point) Cette histoire est-elle sérialisable ? Démontrer.
\begin{correction}
$r_2[x] - w_3[x]$ et $r_3[z] - w_2[z]$. Suffisant pour montrer un cycle.
\end{correction}
\item (2 points) Trouver l'exécution produite par verrouillage à deux phases simple si les verrous d'une transaction sont toujours relâchés après sa validation ou son annulation. Si un interblocage survient, on supprime la plus ancienne transaction (sans la réinsérer) et on continue.
\begin{correction}
$H': r_1[x] r_2[x] r_3[y] r_3[z]$\\
Interblocage, suppression de T1. De nouveau interblocage, suppression de T2. Execution de $C_3$\\
$H'' : r_3[y] r_3[z] w_3[x] c_3$
\end{correction}
\end{enumerate}
